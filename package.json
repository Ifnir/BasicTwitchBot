{
  "name": "basictwitchbot",
  "version": "1.0.1",
  "description": "Basic bot for twitch. My channel is mrifnir",
  "main": "main.js",
  "scripts": {
    "check": "standard && node -r dotenv/config main.js && exit 1",
    "fix": "standard --fix && node -r dotenv/config main.js && exit 1",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon -r dotenv/config main.js"
  },
  "keywords": [
    "basic",
    "bot",
    "twitch"
  ],
  "author": "ifnir",
  "license": "ISC",
  "dependencies": {
    "dotenv": "8.0.0",
    "tmi.js": "^1.4.2"
  },
  "devDependencies": {
    "standard": "12.0.1"
  }
}
