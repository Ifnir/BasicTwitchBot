{
  "name": "basictwitchbot",
  "version": "1.0.1",
  "description": "Basic bot for twitch",
  "main": "main.js",
  "scripts": {
    "test": "standard && node main.js && exit 1",
    "fix": "standard --fix && node main.js && exit 1",
    "start": "node -r dotenv/config main.js"
  },
  "keywords": [
    "basic",
    "bot",
    "twitch"
  ],
  "author": "ifnir",
  "license": "ISC",
  "dependencies": {
    "dotenv": "8.0.0",
    "tmi.js": "^1.4.2"
  },
  "devDependencies": {
    "standard": "12.0.1"
  }
}
